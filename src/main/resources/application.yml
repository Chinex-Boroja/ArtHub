spring:
  #  config:
  #    activate:
  #      on-profile: prod
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    url: jdbc:mysql://localhost:3306/userdb
    password: boroja24

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: 'true'